<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yobi"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".recipe.Activity_recipe_write"
            android:exported="false" />
        <activity
            android:name=".recipe.Activity_recipe_detail_order"
            android:exported="false" />
        <activity
            android:name=".recipe.Activity_recipe_detail"
            android:exported="false" />
        <activity
            android:name=".main.Activity_main"
            android:exported="false" />
        <activity
            android:name=".register.Activity_register"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" /> <!-- editText 사용 시 컴포넌트 가려짐 방지 -->
        <activity
            android:name=".register.Activity_login_normal"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" /> <!-- editText 사용 시 컴포넌트 가려짐 방지 -->
        <activity
            android:name=".register.Activity_login"
            android:exported="false" />
        <activity
            android:name=".splash.Activity_splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>